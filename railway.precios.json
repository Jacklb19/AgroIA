{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.precios"
  },
  "deploy": {
    "startCommand": "python run_prices.py --mode hourly",
    "cronSchedule": "0 * * * *",
    "restartPolicyType": "NEVER"
  }
}
